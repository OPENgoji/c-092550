
import { useTranslation } from '@/hooks/useTranslation';

const GoldenPUFChart = () => {
  const { t } = useTranslation();

  // Chart is now hidden - return null to not render anything
  return null;
};

export default GoldenPUFChart;
